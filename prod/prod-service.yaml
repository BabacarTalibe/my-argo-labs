apiVersion: v1
kind: Service
metadata:
  name: vote
  labels:
    app: vote
spec:
  ports:
  - name: "80"
    nodePort: 30200 #eviter le conflit avec le service de staging
    port: 80
    protocol: TCP
    targetPort: 80
  type: NodePort
